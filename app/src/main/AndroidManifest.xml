<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practice"
        android:usesCleartextTraffic="true">
        <activity android:name=".WebAuth" />
        <activity android:name=".WebAuthPost" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activities.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.ScanActivity" android:label="Escáner"/>
        <activity android:name=".activities.AlbumActivity" android:label="Album"/>
        <activity android:name=".activities.MarketActivity" android:label="Marketplace" />
        <activity android:name=".activities.ProductActivity" android:label="Información del producto"/>
        <activity android:name=".activities.CollectionActivity" android:label="Colección" />
    </application>
</manifest>