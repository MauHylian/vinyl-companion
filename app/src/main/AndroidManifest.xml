<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:allowBackup="true"
        android:configChanges="locale|orientation"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practice"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.ChatListActivity"></activity>
        <activity android:name=".activities.ChatList" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegisterActivity" />
        <activity
            android:name=".activities.ScanActivity"
            android:label="@string/escaner_bar" />
        <activity
            android:name=".activities.AlbumActivity"
            android:label="@string/album_bar" />
        <activity
            android:name=".activities.MarketActivity"
            android:label="@string/marketplace_bar" />
        <activity
            android:name=".activities.ProductActivity"
            android:label="@string/info_del_producto" />
        <activity
            android:name=".activities.CollectionActivity"
            android:label="@string/coleccion_bar" />
        <activity
            android:name=".activities.BrowserActivity"
            android:label="@string/buscar_bar" />
        <activity
            android:name=".activities.ChatActivity"
            android:label="@string/Chat" />

        <activity
            android:name=".activities.CallActivity"
            android:label="@string/Call"
            android:theme="@style/fullscreen"/>
    </application>

</manifest>